{% extends 'base.html.twig' %}

{% block title %}Prevues{% endblock %}

{% block body %}
    <h1>Prevues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prevue.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ prevue.titre }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ prevue.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prevues_index') }}">back to list</a>

    <a href="{{ path('app_prevues_edit', {'id': prevue.id}) }}">edit</a>

    {{ include('prevues/_delete_form.html.twig') }}
{% endblock %}
