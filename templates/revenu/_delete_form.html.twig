<form method="post" action="{{ path('app_revenu_delete', {'id': revenu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revenu.id) }}">
    <button class="btn">Delete</button>
</form>
